import { DisabledAllDirective } from './disabled-all.directive';

describe('DisabledAllDirective', () => {
  it('should create an instance', () => {
    const directive = new DisabledAllDirective();
    expect(directive).toBeTruthy();
  });
});
